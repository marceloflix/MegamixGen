# Git Rules: No Commits or Pushes

- NEVER execute `git commit` or `git push` on behalf of the user.
- The user is strictly in charge of committing and pushing changes.
- Never stage or commit unless the user explicitly requests it in a specific prompt for that single action.
